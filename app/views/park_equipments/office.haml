= render 'menu'
= render 'shared/modal_office'
= render 'shared/modal_branch'
= render 'shared/modal_department'
-#= render 'shared/modal_department'
Ваша организация:
= set_organization(current_user.id)
%br
.form-control
  =form_for @office, url:office_park_equipments_path, remote: true do |f|
    =f.label :number, "Номер кабинета "
    =f.text_field :number
    Выберите филиал:
    =f.collection_select :branch, find_branches, :id, :short_name, prompt: true, remote: true, class: "form-control"
    Выберите отдел:
    =f.grouped_collection_select :department, find_branches, :departments, :short_name, :id, :name, include_blank: true


%a{"data-confirm" => "Are you sure?", :href => "#"} I want to code for 20 hrs today.


%a{"data-background-color" => "#990000", :href => "#"} Paint it red
%a{"data-background-color" => "#009900", "data-text-color" => "#FFFFFF", :href => "#"} Paint it green
%a{"data-background-color" => "#000099", "data-text-color" => "#FFFFFF", :href => "#"} Paint it blue

%a{:href => "#", :onclick => "this.style.backgroundColor='brown'"} Paint it brown
-#-if find_branches.present?
-#  - unless branch_only_one(org_id)
-#    .form-group
-#      Выберите филиал(НЕТ ОБРАБОТКИ СОБЫТИЯ):
-#      =f.collection_select :branch_id,Branch.order(:short_name), :id, :short_name
-#
-#    -#= select_tag :branch, options_from_collection_for_select(@branches, "id", "short_name"),
-#    -#:'data-remote' => 'true',
-#    -#:'data-url' => url_for(controller:'park_equipments', action: 'search_dep'),
-#    -#:'data-type' => 'json', class: 'form-control'
-#    %br
-#    -#-if find_departments(:branch_id).present?
-#    Выберите отдел(ОБРАБОТКА СОБЫТИЯ)
-#    -#=grouped_collection_select(:branch, Organization.order(:short_name), @branches, :short_name, :id, :short_name, include_blank: true, class: 'form-control')
-#    -#=select_tag :department, options_from_collection_for_select(find_departments(1), "id", "name"), class: 'form-control'
-#
-#  - else
-#
-#
-#    -if find_departments_no_branches.present?
-#      Выберите отдел(НЕТ ОБР СОБЫТИЯ)
-#      = select_tag :department, options_from_collection_for_select(find_departments_no_branches, "id", "name"), class: 'form-control'
-#
-#
-#    -#-if @dep_for_org
-#    -#  Отделы:
-#    -#  - @dep_for_org.each do |dep_org|
-#    -#    = dep_org.department_id
-#  -#.pull-right
-#  -#  %br
-#  -#    = link_to " Добавить отдел", "#", {"data-target" => "#extra_dep_fields", "data-toggle" => "collapse"}
-#  -#
-#  -#
-#  -#
-#  -#  #extra_dep_fields.collapsible.collapse
-#  -#    %br
-#  -#    .row
-#  -#      - @departments_all.each do |department|
-#  -#        %table.table.table-striped
-#  -#          %tr
-#  -#            %td
-#  -#              = department.name
-#  -#              = hidden_field_tag :department_id, department.id
-#  -#              = hidden_field_tag :org_id, org_id
-#  -#            -#= hidden_field_tag :branch_id, branch_id
-#  -#
-#  -#            %td
-#  -#              = check_box_tag :check, name: "check"
-#  -#              =hidden_field_tag :act, "add_departments"
-#  -#
-#
-#
-#
-#
-#
-#  = link_to "#", class: "btn btn-primary", "data-target" => "#modal_office", "data-toggle" => "modal" do
-#    Создать кабинет
-#
-#- unless find_branches.present?
-#  %br
-#  %strong
-#    Нет данных об филиалах организации. Для их задания,выберите нужную кнопку:
-#  %br
-#  = link_to "#", class: "btn btn-primary", "data-target" => "#modal_branch", "data-toggle" => "modal" do
-#    Создать филиал
-#  -form_tag(office_park_equipments_path, method: 'post')
-#  = hidden_field_tag :act, 'create_branch'
-#  = hidden_field_tag :organization_id, org_id
-#  = hidden_field_tag :short_name, Organization.find(org_id).short_name
-#  = hidden_field_tag :full_name, Organization.find(org_id).full_name
-#  = hidden_field_tag :address, Organization.find(org_id).address
-#  = submit_tag "В моей организации филиалов нет ", class: "btn btn-primary"
-#
-#-#-if find_offices(70,6).present?
-#-#  %strong
-#    Кабинеты:
-#
-#  %table.table.table-bordered
-#    %tr.info
-#      %th Название
-#      %th Этаж
-#      %th Корпус
-#    %tbody
-#      - find_offices(70,6).each do |office|
-#        %tr
-#          %td
-#            = office.number
-#          %td
-#            = office.floor
-#
-#          %td
-#            = office.block